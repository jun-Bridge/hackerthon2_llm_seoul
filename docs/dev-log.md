# Dev Log — hackerthon2_llm_1
_append-only. 과거 항목은 수정하지 않는다._

## [2026-08-27] bootstrap | polaris
프로젝트 스캐폴드 생성(docs/·.gitignore·.env.example·git init).
기존에 빈 `src/`·`docs/`·`resource/` 폴더만 있던 상태에서 시작.
requirements.md 초안 작성 시작 — 슬롯 전부 TBD.

## [2026-08-27] 스택 확정 · 디렉토리 골격
- 스택: FastAPI(백) + React/Vite/TS(프론트) 분리. LLM은 로컬 Ollama. 배포는 Vercel/클라우드.
- `src/backend/`(app/api·core·llm·schemas·services + tests), `src/frontend/`(src/api·components·hooks·pages·styles) 골격 생성. 빈 디렉토리는 `.gitkeep`.
- README.md에 레이아웃·경계 규약 기록.
- **미해소 충돌 발견**: 로컬 Ollama와 Vercel 배포는 양립 불가(서버리스에 상주 추론 프로세스 불가). requirements 슬롯 9에 선택지 (a)(b)(c)로 기록, 사용자 결정 대기.
- 목적·유스케이스는 사용자 요청으로 공란 유지 → 수용기준 도출 보류.

## [2026-08-27] 스택 재확정 (AWS·Docker·Postgres·Redis·gpt-oss-120b) · 캔버스 기능 추가
- 앞 항목의 "Ollama + Vercel"은 폐기. 확정 스택:
  프론트 React(Vite/TS, Figma 시안) · 백 FastAPI(Python) · AI 로컬 gpt-oss-120b · DB PostgreSQL + Redis · 컨테이너 Docker · 클라우드 AWS.
- 이로써 앞서 기록한 Ollama↔Vercel 충돌은 해소(GPU 있는 자체 호스팅 = 선택지 (a)).
- **레포 경계 확정**: 이 폴더는 **개발 + git 연동만**. AWS 연동·배포·업로드는 **Kiro IDE**에서 별도 처리 → `infra/` 디렉토리 생성했다가 제거, requirements 슬롯 3 Out of scope에 명시.
- 디렉토리 추가: `app/db/{models,repositories}`, `app/cache`, `alembic/versions`, `tests/{unit,integration}`,
  `frontend/src/components/{chat,canvas,common}`, `docker/`.
- 규약 추가: services는 repositories를 통해서만 DB 접근(ORM 모델 누출 금지).
- **미해소**: 캔버스 기능의 실제 동작 정의(편집형/정리형/화이트보드형) — DB 스키마와 프론트 구조를 가르는 갈림길이라 빌드 전 확정 필요. 슬롯 9 기록.
- git remote origin 등록: https://github.com/jun-Bridge/hackerthon2_llm_seoul (push는 미실행 — 사용자 승인 대기).

## [2026-08-27] DB 레이아웃 결정 · 캔버스 정의 확정
- **DB 배치**: `src/db/`로 빼자는 안을 검토했으나 기각. Redis·Postgres 접근 코드는 백엔드가 import하는 파이썬 모듈이라, 백엔드 패키지 트리 밖으로 나가면 별도 패키지 설치(pyproject·editable install)와 Dockerfile 컨텍스트 확장이 따라붙는다 — 배포 단위가 하나인데 소스만 갈라지는 비용. 대신 **`app/db/postgres/`·`app/db/redis/`로 엔진별 독립 모듈화**해 의도한 분리는 그대로 얻었다.
  - 규약: 두 모듈은 서로 import하지 않는다. 함께 쓰는 조합은 `services/`.
  - `app/cache/`는 폐기(→ `app/db/redis/`로 흡수). 캐시도 DB 계층의 한 엔진으로 본다.
- **캔버스 정의 확정: 편집형**(ChatGPT/Claude Canvas형). LLM 생성 문서를 옆 패널에서 사람이 편집 + 선택 영역만 재요청.
  → Postgres 스키마에 문서·버전이 필요. 프론트에 `components/canvas/{editor,revision}` 분리.
  → 파생 미정: 문서 포맷(md/richtext), 버전 보관 정책(스냅샷 vs diff), 동시 편집 여부.

## [2026-08-27] 구조 재검토 (서비스 목표 대조) · 폴더별 문서화 · 브랜치 main
- 브랜치 `master` → `main` 개명 (원격 GitHub 기본값에 맞춤). `gh` CLI는 이 머신에 없음.
- **구조를 목표(채팅 + 편집형 캔버스 + 스트리밍 + PG/Redis)에 대고 재검토 → 구멍 4개 발견, 추가:**
  1. `app/llm/prompts/` — 편집형 캔버스는 "선택 영역 재작성" 프롬프트가 핵심 자산이다. 서비스 코드에 문자열로 박히면 문구 수정이 로직 변경이 된다.
  2. `frontend/src/store/` — 대화와 캔버스가 상태를 공유한다("문서로 만들기" → 캔버스 열림). prop drilling으로는 안 된다.
  3. `frontend/src/types/` — 백엔드 `schemas/`와 짝. 없으면 컴포넌트마다 응답 모양을 추측한다.
  4. `frontend/src/assets/` — Figma 익스포트 원본(`resource/`)과 번들 에셋을 분리.
- **폴더별 `README.md` 28개 작성.** 각 폴더가 무엇을 구현할지 + 지켜야 할 경계 규칙. 루트 README 트리는 지도, 폴더 README가 계약.
- requirements 슬롯 6(데이터·계약) 초안 기입: conversation/message, document/document_version, HTTP API 목록, LLM 인터페이스.
- **핵심 규칙 확정**: 문서 편집은 항상 새 버전을 만든다. 복원도 새 버전. 원본 불변 — 백엔드(`canvas_service`)와 프론트(`canvas/`) 양쪽 README에 동일하게 명시.
- 새로 드러난 미정: 인증·사용자 모델(문서 소유자 식별), 문서 본문 포맷, 버전 보관 정책(스냅샷 vs diff).
- git remote를 HTTPS → SSH(`git@github.com:jun-Bridge/hackerthon2_llm_seoul.git`)로 변경. 이 머신에 자격증명 헬퍼가 없어 HTTPS push가 막혔고, `~/.ssh/id_ed25519`가 이미 jun-Bridge로 인증됨. `main` push 완료.

## [2026-08-27] 요구사항 1차 정리 (브레인스토밍 받아적기)
- **서비스 정의**: 대화하며 LLM과 문서를 만드는 웹 툴. LLM은 문서를 **소유하지 않고 함수(tool call)로 읽고 쓴다.** 사용자가 자유롭게 편집해도 LLM이 항상 최신을 읽는다는 것이 이 설계의 요점.
- **쳐낸 것**: 카카오 연동, 팀 기능, 사용자 맞춤·성향 추론, 테마, 이미지, 세션 간 크로스 참조. "오직 툴" 방향.
- **블록 id 채택**: LLM이 문자 offset으로 위치를 가리키면 사용자가 그 사이 편집한 순간 어긋난다. 안정적 블록 id로 가리켜야 자유 편집과 LLM 수정이 공존한다.
- **편집 잠금(세마포어)**: LLM 쓰기 도구 호출 시 문서를 잠그고 프론트는 블러 + 입력 차단. 턴 종료 시 해제. 실패·연결 끊김에도 영구 잠기지 않도록 타임아웃 필수 — 이걸 수용기준에 넣었다.
- **세션 컨테이너 설계**: 요청마다 Redis에서 복원하는 비상주 컨테이너. 최근 메시지 버퍼 + `context` 요약 문자열을 들고 가고, 버퍼가 차면 absorb LLM이 title/context를 재생성한 뒤 버퍼를 비운다. 사용자가 제목을 직접 정하면 덮어쓰지 않는다.
- 컨테이너는 **문서를 상태로 소유하지 않는다.** 필요하면 tool로 읽는다 → 사용자가 문서를 고쳐도 stale해지지 않는다.
- 수용기준을 M0~M4 마일스톤으로 분해, 전부 테스트·수치로 판정 가능하게 작성.

## [2026-08-27] 저장 모델 통일 · 일괄 서식 · 문서 작업
- **두 코어의 저장 방식을 하나로 맞췄다**: 대화·문서 모두 Redis에 작업 사본을 두고 편집하다가, 트리거마다 Postgres에 통째로 덧씌운다(플러시). 상주 객체 없이 요청마다 복원.
  - 트리거: LLM 턴 종료 · 사용자 편집 후 debounce · 명시 저장/다운로드/일괄 서식 · 세션 이탈
  - 플러시는 전체 덮어쓰기. 부분 병합 안 함 — 작업본이 정답이므로.
  - **대가**: 마지막 플러시 이후 Redis가 죽으면 그 구간 편집은 사라진다. 반응성과 맞바꾼 것. 허용 구간은 debounce 간격이 정한다.
- **문서 포맷 = 마크다운** 확정. 다운로드(.md)·복사·잘라내기·붙여넣기·실행취소 지원.
- **일괄 서식 버튼**: "서식 제거"/"서식 적용". 버튼 한 번에 LLM이 문서 전체를 읽고 고친다. 채팅 턴을 거치지 않고 단독 실행하되, 진행 중 잠금·블러는 동일.
- **구조 재편**: `services/` 한 덩어리를 없애고 `app/auth/`·`app/chat/`·`app/document/`를 나란히 세웠다. 요구사항의 "두 개의 코어"가 디렉토리에 그대로 보이도록.
  - `app/llm/tools/` 추가 — 도구 스키마 + executor. executor는 오류를 예외로 터뜨리지 않고 LLM에 결과로 돌려준다(LLM이 스스로 고치게).
  - `canvas/revision/` 제거(버전 이력은 범위 밖) → `canvas/toolbar/` 추가(문서 작업·일괄 서식).
- 문서에서 외부 참조 프로젝트 언급을 전부 걷어냈다. 이 프로젝트 설계만 남긴다.

## [2026-08-27] 편집 제안·승인 모델 도입 · 줄 단위 확정 · 동시 접속 차단
- **LLM에게서 쓰기 권한을 뺐다.** 도구 호출은 제안 버퍼에 쌓일 뿐이고, 문서 코어가 diff(+/−)를 만들어 보여준 뒤 사용자가 **적용**을 눌러야 반영된다. 거절하면 문서는 손대지 않는다.
  - 이 구조 하나로 "LLM이 사용자 편집을 통째로 날린다"는 위험이 원천 소멸한다.
  - 제안 대기 중에는 편집을 잠근다 — 그 사이 사용자가 고치면 제안이 어긋나므로.
  - **적용/거절 결과가 다음 턴 LLM 컨텍스트에 들어간다.** 거절된 걸 또 내밀지 않게. 거절 사유도 선택적으로 전달.
  - **경계 유지**: 문서 코어는 여전히 대화 코어를 모른다. accept/reject 라우트가 두 코어를 각각 호출하고 결과를 대화 코어에 기록한다.
- **블록 = 마크다운 한 줄** 확정. 빈 줄도 블록. diff 단위와 편집 단위가 같아져 계산·표시·되돌리기가 한 좌표계에서 돈다. 붙여넣기 분할 문제도 자동 해소(개행으로 자름).
- **편집 이력 = Redis 스냅샷 스택.** 별도 버전 테이블 없음. LLM 편집 적용 직전 스냅샷은 필수.
- **동시 접속 원천 차단** — 두 겹:
  1. 계정당 활성 접속 하나. 새 로그인이 기존 접속을 밀어낸다.
  2. 이미 열린 세션에 접근하면 블러 + "다른 곳에서 작업 중" 읽기 전용.
  LLM 편집 잠금과 같은 오버레이를 재사용하고 사유 문구만 다르게.
- 슬롯 10을 재편: 결정 필요 / 실측 후 정할 값 / 확정된 것(표)으로 갈랐다. 해소된 항목이 열린 항목과 섞여 읽히지 않던 문제.
- 새로 드러난 열린 항목: 코드 펜스·표 같은 **여러 줄 마크다운 구조**를 LLM이 한 줄만 고쳐 깨뜨릴 위험.

## [2026-08-27] 구조 적대적 리뷰 — 결함 수정
팀 배포용 계획서 기준으로 requirements를 통독하고 구조 결함을 잡았다.

- **잠금 진실원천이 둘이었다** — Postgres `document.locked_by/locked_at` 컬럼과 Redis `doc:{id}:lock`이 동시에 존재. 잠금·제안·이력은 전부 휘발성이므로 **Redis 단일**로 정리하고 PG 컬럼을 없앴다. Postgres에는 확정 본문만.
- **오케스트레이터를 구조에 명시했다.** "두 코어는 서로 모른다"는 원칙을 세워놓고, 실제로는 걸치는 동작이 셋 있었다 — 승인 결과를 대화에 기록, 일괄 서식이 LLM 호출, 잠금 수명 관리. 이걸 어느 코어에 넣어도 원칙이 깨지므로 **제3의 조율자**를 세웠다. `app/orchestrator/` 신설.
  - 오케스트레이터는 **상태를 소유하지 않는다.** 순서와 수명만 관리.
- **SSE 이벤트 계약을 표로 명시했다.** 채널 하나에 토큰·도구·잠금·제안·문서변경·세션메타가 전부 흐르는데 이벤트 종류가 정의돼 있지 않았다. 팀이 나뉘어 작업하면 프론트·백이 각자 다른 이름을 쓴다. 채널을 세션 단위 하나로 못박은 이유도 적었다(순서 보장).
- **도구 호출 루프에 상한이 없었다.** read → propose → read … 무한 반복 가능. 턴당 상한을 필수로 하고 수용기준에 넣었다.
- **API 중복**: `blocks/{id}`와 `lines/{id}`가 동시에 있었다. 줄 단위 확정에 맞춰 `lines`로 통일.
- **없어진 경로 참조**: LLM 인터페이스 절이 `services/canvas_service.py`를 가리키고 있었다(services는 폐지됨). 도구 실행 주체를 문서 코어로, 중계를 오케스트레이터로 바로잡았다.
- **공통 에러 규약 표 추가** — 401/403/404/409/422. 팀이 나눠 만들 때 응답 코드가 제각각이 되는 걸 막는다.
- **세션 삭제 시 Redis 잔재 정리**가 수용기준에서 빠져 있었다(PG만 검증). 추가.
- **동시 접속 두 겹의 근거**를 적었다 — 1번(계정 단일 접속)은 정책, 2번(세션 점유)은 문서에 손대는 시점의 자물쇠. 밀려난 화면이 그 사실을 알기까지의 시차와 탭 복제를 2번이 막는다.
- **제안 결과 기록 형태**: `message.role`에 `system`을 두고 거기 남긴다. `tool` role은 쓰지 않는다.
- **order 재정렬 규칙**: 작업본에서는 리스트 순서가 곧 order, 플러시 때 0부터 다시 매긴다. 삽입마다 뒤를 미는 연산 없음.
- Out of scope 모순 정리 — 이미지는 확정 제외인데 TBD로 적혀 있었다. 회원 탈퇴·비밀번호 재설정도 범위 밖으로 명시.

## [2026-08-27] 인증·제안 동시성·absorb 실행 확정
- **인증 = HttpOnly 쿠키 세션 + Redis.** 결정 근거는 SSE다. `EventSource`는 커스텀 헤더를 못 붙여서, JWT Bearer로 가면 토큰을 쿼리스트링에 넣거나(URL·프록시 로그에 남음) fetch 기반 스트리밍을 직접 구현해야 한다. 쿠키는 자동으로 실린다. 부수로 XSS에 토큰이 안 새고, 서버가 세션을 지울 수 있어 **단일 접속 강제가 즉시** 먹는다. 대가는 CSRF 대비(SameSite=Lax + 상태변경 요청 보호).
- **살아 있는 제안은 최대 하나.** 제안이 떠 있으면 문서 편집뿐 아니라 **채팅 입력도 막는다.** 이 불변식 하나로 제안 큐·순서·충돌 처리가 통째로 사라진다. 제안 중 `POST /messages`는 409.
- **absorb는 백그라운드.** 턴 응답을 먼저 끝내고 뒤에서 돈다. 완료되면 SSE `session_meta`로 제목을 민다.
  - **실패 처리를 명시**: 실패해도 대화는 멀쩡해야 한다. 기존 title·context 유지, 버퍼도 비우지 않음(다음 기회 재시도). 수용기준에 넣었다.
  - absorb 중 다음 턴이 시작될 수 있으므로 **세션 단위 직렬화** 필요.
