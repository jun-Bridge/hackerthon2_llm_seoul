# UniVoice 백엔드 의존성

# 웹 프레임워크
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# 데이터 계층 (B) — PostgreSQL / Redis
psycopg[binary,pool]>=3.1.0
redis>=5.0.0

# 인증 (A) — 비밀번호 해시
bcrypt>=4.1.0

# LLM (C) — AWS Bedrock
boto3>=1.34.0

# 이미지 첨부 민원 — 서버에서 리사이즈/재인코딩 (프론트는 원본만 보낸다)
Pillow>=10.2.0

# 테스트
pytest>=8.0.0
