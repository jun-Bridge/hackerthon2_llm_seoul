# db/ — 데이터 계층 (엔진별 모듈)

- `postgres/` — 영속. 대화·문서·버전
- `redis/` — 휘발. 세션·캐시·스트림 버퍼

**규칙: 두 모듈은 서로 import하지 않는다.** 둘을 함께 쓰는 조합(예: 문서 저장 후 캐시 무효화)은 `services/`가 조율한다.

`services/` 밖에서는 이 계층을 직접 호출하지 않는다 — 라우트가 repository를 부르지 않게.
