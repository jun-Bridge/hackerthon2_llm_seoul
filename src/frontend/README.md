# src/frontend/ — React + Vite + TypeScript

개발: `npm run dev` (기본 5173) · 빌드: `npm run build`
백엔드 주소는 `.env`의 `VITE_API_BASE_URL`.

화면 구성: **좌측 대화 · 우측 캔버스(문서 편집)** 2단 워크스페이스.
UI 시안은 Figma 기준(`resource/`에 원본).

구현 예정 (루트)
- `index.html` · `vite.config.ts` · `tsconfig.json` · `package.json`
