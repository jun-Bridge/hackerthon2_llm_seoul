# store/ — 전역 상태

대화와 캔버스가 **같은 상태를 공유**하기 때문에 필요하다 (대화에서 "문서로 만들기" → 캔버스가 열림, 캔버스 편집 → 대화 맥락에 반영).

구현 예정
- `chatStore.ts` — 현재 대화, 메시지 목록, 스트리밍 진행 상태
- `documentStore.ts` — 열려 있는 문서, 현재 버전, 미저장 변경
- `uiStore.ts` — 패널 분할 비율, 열림/닫힘

라이브러리는 미정(Zustand 유력). 서버 응답 캐싱까지 필요해지면 그때 재검토.
