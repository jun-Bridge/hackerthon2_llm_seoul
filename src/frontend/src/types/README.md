# types/ — 공유 타입

백엔드 `app/schemas/`와 **짝을 이루는** TypeScript 타입. 한쪽을 바꾸면 다른 쪽도 바꾼다.

구현 예정
- `chat.ts` — 메시지, 스트림 청크
- `document.ts` — 문서, 버전, 선택 영역 편집 요청
- `api.ts` — 공통 에러·페이지네이션 형태

컴포넌트가 API 응답 모양을 각자 추측하지 않게 하는 것이 목적.
