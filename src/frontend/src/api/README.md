# api/ — 백엔드 클라이언트

컴포넌트가 `fetch`를 직접 부르지 않게 한다. 엔드포인트 변경이 여기서 끝나야 한다.

구현 예정
- `client.ts` — base URL·헤더·에러 정규화 래퍼
- `chat.ts` — 메시지 전송, **SSE 구독**(토큰 스트리밍, 재연결 처리)
- `documents.ts` — 문서 CRUD, 버전 목록·복원
- `canvas.ts` — 선택 영역 편집 요청

응답 타입은 `types/`에서 가져온다. 여기서 새로 정의하지 않는다.
