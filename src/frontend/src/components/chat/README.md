# components/chat/ — 대화 UI

구현 예정
- `MessageList.tsx` — 스크롤·자동 하단 고정
- `MessageBubble.tsx` — 역할별 렌더, 마크다운 표시
- `Composer.tsx` — 입력창, 전송, 중단
- `StreamingMessage.tsx` — 토큰 도착에 따른 점진 렌더

"이 내용을 문서로" 액션이 캔버스를 연다 — 문서 생성은 `store/`를 통해 캔버스 쪽과 연결.
