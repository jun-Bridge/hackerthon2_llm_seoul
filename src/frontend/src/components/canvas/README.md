# components/canvas/ — 문서 편집(캔버스)

**편집형**: LLM이 만든 문서를 사람이 직접 고치거나, 선택 영역만 LLM에 재요청한다.

- `editor/` — 본문 편집과 선택 영역 처리
- `revision/` — 버전 히스토리·비교·복원

편집 결과는 항상 **새 버전**으로 쌓인다. 원본을 덮어쓰지 않는다(백엔드 `canvas_service`와 동일 규칙).
